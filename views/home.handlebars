<h1>Work space</h1>
{{#if error}} <span>{{error}}</span>{{/if}}

<div>
  <h3>To do: </h3>
    <ul class="todo-list">
        {{#each tasks}}
        <li>Task: {{this}}</li>
        {{/each}}
    </ul>
    <ul class="todo-list">
        {{#each obrisia}}
        <li>Task: {{this.name}}</li>
        {{/each}}
    </ul>
</div>







{{!-- <form class="form-inline" method="POST" action="/user/search">

  <div class="form-group">
    <input type="text" name="id" class="form-control" placeholder="Add new task">
  </div>

  <input type="submit" class="btn btn-primary" value="Add task">

</form> --}}
